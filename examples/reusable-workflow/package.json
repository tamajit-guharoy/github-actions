{
  "name": "reusable-workflow-example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "build": "mkdir -p dist && cp index.js dist/"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^30.0.0"
  }
}
